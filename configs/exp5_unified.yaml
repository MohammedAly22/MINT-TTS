# Experiment 5 -- Unified linguistic + acoustic routing with a hardware budget.
# This is the full formulation: C*(x, q, h).
_base_: base.yaml

log:
  run_name: exp5_unified
  wandb_tags: [adaptive, unified, budget]

model:
  encoder: { routing: token, share_weights: true }
  decoder: { routing: token, share_weights: true }

loss:
  compute:
    use_hardware_cap: true
    hardware_weight: 0.75
    lambda_max: 0.06
