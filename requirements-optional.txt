# Optional extras. None of these are needed for the first experiment.
wandb>=0.16                # second logging backend, keeps plotly figures interactive
huggingface_hub>=0.20      # scripts/download_vocoder.py --hf-repo
faster-whisper>=1.0        # eval.asr_backend: whisper
pyworld>=0.3               # higher quality F0 than the built-in extractor
pytest>=7.4                # test suite
pyflakes>=3.0              # lint

# Interactive figures (log.figure_backend: plotly). Hovering a heatmap cell
# shows token/word/depth. Perfect with wandb, which renders plotly natively.
# For TensorBoard these also need Chrome, installable with `plotly_get_chrome`.
plotly>=6.1.1
kaleido>=1.0
pillow>=10.0

# Egyptian Arabic experiment (configs/egyptian_*.yaml).
#   transformers -- the frozen MARBERTv2 that supplies contextual word vectors.
#     Required to PREPROCESS and to SYNTHESISE with semantics enabled; the
#     training loop itself never touches it, because the vectors are cached.
#   huggingface_hub / librosa -- scripts/prepare_egyptian.py, and the
#     BigVGAN vocoder (its bundled mel helpers import librosa). Note it
#     does NOT use `datasets`: that repo stores 15,653 individual .wav files,
#     so load_dataset() fetches them one request at a time, hits the Hub's
#     rate limit, and downloads everything before any --limit could apply.
#   arabic-reshaper + python-bidi -- correct Arabic rendering in matplotlib
#     figures. Without them the heatmap labels appear reversed and unjoined.
transformers>=4.40
librosa>=0.10
arabic-reshaper>=3.0
python-bidi>=0.4
